# Optional: read-only token for cross-repository Sigma discovery.
# Prefer a least-privilege GitHub App installation token with metadata/contents/issues/pull-request read access.
# Do not grant write, administration or production permissions to the phase-1 discovery runner.
SIGMA_GITHUB_TOKEN=
